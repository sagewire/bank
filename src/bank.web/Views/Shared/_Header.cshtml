@model HeaderViewModel
@{ 

}
<div class="full-bg" style="background-image: url('@Model.HeaderImage')">
    <div id="header">

        <div class="container">
            <div class="surround big-text">
                <h1 class="display-6">@Model.HeaderTitle</h1>
                @if (Model.HasSubTitle)
                {
                <h4>
                    <i>
                        @Model.SubTitle
                    </i>
                </h4>
                }
            </div>
        </div>
    </div>
</div>