@using bank.reports
@using bank.reports.charts
@model TableElement

@if (Model.Orientation == TableOrientation.Horizontal)
{
    @Html.Partial("template/table-horizontal/_TableElement", Model)
}
else
{
    @Html.Partial("template/table-vertical/_TableElement", Model)
}
